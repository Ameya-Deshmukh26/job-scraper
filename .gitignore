# Database & logs
seen_jobs.db
*.db
*.log
job_scraper.log

# Personal profile & apply data
profile.json
autoapply/applied_*.json

# Generated resume files
*.pdf
*.aux
*.out
*.fls
*.fdb_latexmk
tailored_resumes/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets — never commit real keys
# config.py is safe to commit because all keys are empty strings.
# If you add real keys, use env vars or a .env file (which is listed below):
.env
.env.*
secrets.json

# MCP config - contains live API tokens, never commit
.mcp.json

# Regenerable caches (rebuild with: python discover_boards.py --fetch-yc,
# and sources/h1b.py downloads its own on first use)
data/yc_companies.json
data/h1b_cache.pkl
data/*.log
server.log
server_err.log

# Stray one-off scripts kept out of the package surface
check_db.py
check_linkedin.py
filter_linkedin.py
test_headed*.py
